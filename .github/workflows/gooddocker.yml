name: Docker Workflow

on:
  - push
  
jobs: 
  docker_build: 
    runs-on: ubuntu-latest
    steps: 
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Docker Build 
      run: docker build -t albertarko/resume-app:latest .

    - name: Docker Login
      run: docker login --username=albertarko --password=dckr_pat_1cxrYa3FhR-MdnSjy5Sf7BaXpgs

    - name: Docker Publish 
      run: docker push docker.io/albertarko/resume-app:latest

  docker_deploy:
    needs: docker_build
    runs-on: ubuntu-latest 
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      - name: Docker Login
        run: docker login --username=albertarko --password=dckr_pat_1cxrYa3FhR-MdnSjy5Sf7BaXpgs
      - name: Docker Run
        run: docker run -d -p 8080:80 docker.io/albertarko/resume-app:latest